# ==============================================================================
# ふりぃらじかるず Backend — Python 依存パッケージ
# バージョンは再現性のため固定（セキュリティ規定 第3項）。
# ==============================================================================

# ----- Web フレームワーク / ASGI サーバ -----
# F-4 セキュリティ更新（2026-07-21）: starlette@0.41.3 の既知脆弱性
# （ReDoS / SSRF / リソース枯渇 / 参照解決）を解消するため fastapi を更新。
# 0.133.0 以降は starlette の上限ピンが撤廃され starlette 1.x を許容する。
fastapi==0.139.2
# F-4: fastapi(>=0.133) が許容する starlette の脆弱性解消版を明示ピン。
starlette==1.3.1
uvicorn[standard]==0.51.0

# ----- 設定管理 -----
pydantic-settings==2.14.2

# ----- DB（SQLAlchemy 2.0 系 + MySQL ドライバ + マイグレーション） -----
SQLAlchemy==2.0.51
PyMySQL==1.2.0
alembic==1.18.5

# ----- LLM / 検索（KRE から利用） -----
openai==1.59.6
azure-search-documents==11.5.2

# ----- ナレッジグラフ -----
networkx==3.6.1

# ----- KRE 設定ローダ（kre/config/loader.py が retrieval_config.yaml を読む） -----
PyYAML==6.0.3

# ----- 認証（Google Identity Services の ID トークン検証） -----
google-auth==2.56.0

# ----- 観測性（構造化ログ） -----
structlog==24.4.0

# ----- テスト -----
pytest==8.3.4
httpx==0.28.1

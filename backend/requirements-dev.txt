# ==============================================================================
# ふりぃらじかるず Backend — 開発・テスト用の依存パッケージ
#
# 本番イメージには入れない（Dockerfile は requirements.txt のみを pip install する）。
# セキュリティ監査 F-11（2026-07-26）で、本番コンテナにテストフレームワークが
# 同梱されていた問題を解消するために分離した。
#
# 使い方:
#   pip install -r requirements-dev.txt   # 本番依存も含めて一括で入る
# ==============================================================================

# 本番依存を取り込む
-r requirements.txt

# ----- テスト -----
# PYSEC-2026-1845（pytest 8.3.4）の修正版。監査 F-11 で 8.3.4 → 9.0.3 へ更新。
pytest==9.1.1
# FastAPI/Starlette の TestClient が内部で利用する（本番コードからは import しない）。
httpx==0.28.1
